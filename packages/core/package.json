{
  "name": "@gate/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "Runtime-agnostic GATE token exchange: OIDC validation, two-layer authorization, GitHub App client, revocation, audit.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p test/tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "jose": "catalog:",
    "re2js": "catalog:",
    "yaml": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@gate/testkit": "workspace:*",
    "@types/node": "catalog:",
    "vitest": "catalog:"
  }
}
